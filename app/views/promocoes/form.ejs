<html>
    <body>
        <form action='/promocoes' method="post">
        <div>
            <input type="text" name="mensagem"/>
        </div>
        <div>
            <select name="livro[id]"> <!-- aqui usamos colchetes pq é o padrao do bodyparser -->
                <%for(var i=0;i<lista.length;i++){%>
                    <option value="<%=lista[i].id%>"><%=lista[i].titulo%></option> <!-- estamos usando cod dinamico para o value da opcao e pra lista de livros -->
                <%}%>
            </select>
        </div>
        <input type="submit" value="Cadastrar promoção"/>
    </body>
    
</html>